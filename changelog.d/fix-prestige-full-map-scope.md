### 修复

- **归隐声望覆盖全部五图**：地图 4/5 在 MVP-2 加入后，声望经济一直停留在三图口径——Boss 4/5 无里程碑值、精英与全通判据只统计地图 1–3，导致推到地图 5 与打完 Boss 3 就归隐拿到完全相同的声望，深推毫无回报。现补 Boss 4/5 里程碑（40 / 60，基础声望 100→200），精英与全通判据改为覆盖 48 关。取值锚点：深推轮长约为三图轮两倍，基础声望同步翻倍以保持「声望 / 分钟」恒定。
- **表现加成触顶不再显示错值**：精英数从 5 个增至 12 个后，归隐演出的分项之和最高可达 +58%，与实际生效的 +30% 对不上。新增「表现加成封顶 +30%」行，仅在分项之和超顶时显示。

### 变更

- **全通判据改为 48 关**：`fullClear` 从三图 28 关改为五图 48 关（`economy.md` §1.2）。三图轮因此不再获得 +10% 全通加成，每轮结算 130 → **120**；五图轮为 260。`economy.md` §3 收支预期按轮型分列重算，四轮买空商店的节奏不变（购买顺序有微调）。
- **声望判据数据源**：`enemies.ts` 新增 `allStages()`（三图手写 + 地图 4/5 曲线生成合并），`prestige.ts` 的 `ELITE_KEYS` / `TOTAL_STAGES` 由其派生，不再只看地图 1–3 的 `STAGES`。
- **冻结文案更新**：`copy/retire.md` v1.2 补 Boss 4/5 里程碑行、「三图全通」改「五图全通」、新增加成封顶行；既有行逐字不动。sim `mvp0_sim.py` 的声望常量同步（该 sim 只建模三图 28 关，故 `fullclear` 恒 False，其输出即三图轮口径）。
