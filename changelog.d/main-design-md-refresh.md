### 变更

- **DESIGN.md 回灌修炼面板视觉语言**（R2 获批后的强制步骤，走 impeccable document 合并模式，既有章节口径未动）：
  - **§5 signature component 替换**：「周天充能条」已被新方案取代，改写为「修炼面板」——外视/内视两态、三层合成、气光的三层叠法（multiply 暗晕 + screen 气光 + 径向遮罩羽化）、波形 mask 液面、以及「禁止在人影上布可数窍穴点」的层级分工。
  - **§2 Tertiary 扩容**：由「战斗语义色」放宽为「语义状态色」，下分战斗组（毒/血/盾/暴）与修炼组，新增 **朱砂 cinnabar**（`oklch(0.68 0.09 38)`，窍穴可冲态）与 **墨金 ink-gold**（`oklch(0.74 0.075 76)`，已成态）。墨金与 candle-gold 分工写死：暖金标进展，墨金标既成。The Semantic Ink Rule 同步覆盖修炼组。
  - **§4 The Midnight Rule 裁决**：发光授权由「按组件」改为「按场景」发放，新增第三项特权「内景场景」（丹田内视整场授权发光）；外视人影仍受日常约束，只许丹田一处气光。此前规则明文禁止日常界面 glow，与新面板正面冲突。
  - **§6 新增四条**：AI 素材只供质感、循环视频接缝验收标准（Do）；禁止给 `<video>` 或其祖先挂 CSS filter、禁止用位图素材承担连续变化的数据显示（Don't）。
  - frontmatter 补 `cinnabar` / `ink-gold` 两个 token。

### 修复

- **原型色值不自洽**：`zhoutian-composite-v3.html` 里朱砂有三个互不相同的取值（`0.62 0.19 32` 已 0 引用的死 token、图例 `0.56 0.10 42`、窍穴名 `0.68 0.09 38`），墨金有两个（`0.74 0.075 76` / `0.74 0.055 78`）。统一为 `--cinnabar` / `--ink-gold` 两个变量，各处改引用变量，DESIGN.md 照抄实际取值——避免文档与代码各写一套。
