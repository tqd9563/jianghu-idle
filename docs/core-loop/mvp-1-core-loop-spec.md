# 《江湖无尽录》MVP-1 核心循环规格书

> **状态声明（先于一切内容）**：
> 1. **本文是草案（v0.1），未定稿**。MVP-1 的**任何实现**（代码 / 数值表落地 / UI）以「**MVP-0 测试报告出具且无 §10.2 红旗**」为开工门禁——门禁未开前，本文只允许纸面迭代。
> 2. 本文写作期间 MVP-0 处于测试期，**不触碰任何冻结件**（《测试执行方案》§7 清单：全部数值表、诊断文案、归隐流程文案、节点与路线卡文案）。本文对冻结件只引用、不修改。
> 3. 目录约定：MVP-0 文档不动、不归档；MVP-1 文档在 `docs/core-loop/` 同目录以 `mvp-1-` 前缀并列（文件名前缀即命名空间），MVP-0 测试报告落地后再议归档。
>
> **版本**：v0.1（草案）
> **日期**：2026-07-06
> **适用范围**：MVP-1 放置节拍验证原型
> **文档目的**：定义 MVP-1 的头号假设、范围边界、会话结构锚点与成功/失败标准，作为数值表重推批次与后续交付物的上位输入。
> **上游文档**：`mvp-0-core-loop-spec.md` v0.10（§1.2 / §6.1 / §10.3 / §11.2）、`mvp-0-scope-boundary.md`（重推方法论与规则层清单）、`mvp-0-playtest-plan.md` §7（冻结纪律）、`mvp-0-telemetry-spec.md`（范围纪律）、`mvp-0-reputation-economy.md` §5（平台期衔接点）、初案 `docs/overview/game-design-proposal.md` §十四（节奏与会话结构，本文会话锚点的讨论起点）、`docs/directions/` 五份方向记录（B/C 表出处）。
> **重要约束**：本文只定义 MVP-1，不重写完整游戏愿景，不设计商业化，不假设大团队。MVP-0 已交付系统全量保留为基底，MVP-1 是在其上**加一层离线节拍**，不是重做。

---

## 0. 变更日志

| 版本 | 日期 | 变更内容 |
|---|---|---|
| v0.1 | 2026-07-06 | 草案新建：锁定头号假设（放置节拍）、范围三表（锁定/触发/不做）、会话结构锚点（全部 `[待定-重推批次]`）、成功/失败标准草案、交付物路线图。写作依据为三问裁决（所有者已批准：纸面并行、实现门禁）。 |

---

## 1. MVP-1 设计目标

### 1.1 头号假设：放置节拍

MVP-1 的唯一核心目标是验证以下节拍是否成立：

```text
离线收益 → 回归检查 → 决策投入
```

**文档依据**：MVP-0 规格书 §1.2 特别声明原文——

> 「放置节拍是尚未验证的第二核心假设：MVP-0 是一个 30–60 分钟的**主动**循环验证，完全不包含离线收益与回访节奏。『离线收益 → 回归检查 → 决策投入』的放置节拍是独立假设，MVP-0 通过**不构成**该假设成立的证据（**等 2 分钟资源与隔 8 小时回访是两种不同的心理模型**），该假设由 MVP-1 的基础离线收益单独验证。」

同口径见 MVP-0 规格书 §10.3（放置/离线节拍列为「MVP-0 不用验证的指标」）与量级边界文档「节奏模型」行（「离线/回访节拍是品类核心；属独立假设，归 MVP-1 单独验证」）。

### 1.2 假设边界（防止验证目标漂移）

- **验证的是心理模型，不是功能**：离线收益作为功能实现容易，本假设问的是——玩家离开后**是否愿意回来**、回来后**是否看得懂发生了什么**、看懂后**是否有事可决策**。三环缺一，放置节拍不成立。
- **MVP-0 结论的效力边界**：MVP-0（待外部测试确认后）证明的是**主动 prestige 循环成立**；量级边界文档明文「MVP-0 测试通过 ≠ 品类成立：它证明的是主动 prestige 循环成立；放置/离线的习惯循环是另一个假设，MVP-1 必须单独验证」。本文不得引用 MVP-0 任何数据作为放置节拍成立的证据。
- **玩家此刻应该感到**（类比 MVP-0 §1.1）：
  1. **离开时**：我可以放心走，闭关会替我积累，且不会替我做重要决定。
  2. **回来时**：一眼看懂闭关期间发生了什么、拿到了什么。
  3. **结算后**：我有一件值得做的事（投入、突破、挑战、调整），不是「点一下就走」。
  4. **形成节奏后**：我开始预期「下次上线时会攒到 X，就能做 Y」——回访有了目标。

### 1.3 设计支柱

沿用 MVP-0 规格书 §1.3 五支柱（放置可理解 / 卡点有原因 / 调整有结果 / 归隐值得做 / 范围可实现），新增一条：

| 支柱 | MVP-1 表达方式 | 判定标准 |
|---|---|---|
| 回访有决策 | 出关结算后必然衔接至少一个真实决策点 | 玩家无「上线只点一下就走」的空会话（初案 §十四明文禁止的空会话设计） |

---

## 2. MVP-1 范围

### 2.1 明确包含（文档已锁定项）

| 项 | 内容 | 锁定依据 |
|---|---|---|
| 基础离线收益 | 头号假设的唯一载体，规则见 §5 | MVP-0 规格书 §1.2 / §10.3 / §11.2 |
| 「闭关—出关结算」呈现皮肤 | 离线收益的玩家侧术语体系 | MVP-0 规格书 §6.1 v0.8 注记：「『闭关』一词刻意不用于突破呈现，保留给 MVP-1 离线收益作皮肤（『闭关—出关结算』），术语与数值同样做预算管理」 |
| 离线结算 / 回归检查视图 | 出关结算与回访落地的界面 | 量级边界文档 UI 节：「离线结算/回归检查视图是 MVP-1 的**新增**界面，不是现有界面的改造」 |
| 数值表换锚点重推 | 全部数值表按新轮长锚点重推，规则层不动 | 量级边界文档「对后续工作的含义」第 2 条：「不修改 MVP-0 各表的数字去『放大』，而是换锚点（新的轮长目标）后按各表头部的『推导锚点』重推一遍；规则层原样沿用」 |
| 声望商店平台期衔接 | 第 4 轮起商店买空后的追加节点/内容 | 《声望经济表》§5：「第 4 轮起进入平台期（MVP-1 的内容衔接点）」；《模拟报告》同口径 |
| MVP-1 埋点另立规格 | 不在 MVP-0 埋点文档上扩建 | 《埋点与度量规格》范围纪律：「MVP-1 若需要长期数据体系，另立规格，勿在本文上扩建」 |

### 2.2 触发条件项（条件满足才评审进入，不默认做）

| 项 | 触发条件 | 出处 |
|---|---|---|
| 周天圆满小幅属性加成 + 动态周天数 N | MVP-1 数值表重推时作为候选评审；须逐条过该文四锁定点（加法合并禁独立乘区 / 锚定新高水位 / 突破清零吸收 / N 线性等差封顶九转段间换皮）与规则层检查清单 | `docs/directions/zhoutian-direction-note.md` §5/§6 |
| 归隐结算打字机演出 | 评审输入 = MVP-0 首测的兑现时延分布 + 访谈中对结算演出的主观反馈；四锁定点见该文 §3 | `docs/directions/retire-typewriter-direction-note.md` §3/§4 |
| 推荐境界 tooltip（方案 a） | 推荐先做；措辞红线「只描述诊断行为，不得出现『达到即可通关』类胜利承诺」 | `docs/directions/recommended-realm-direction-note.md` §3 |
| 推荐境界改「参考境界」（方案 b） | 仅当 MVP-0 首测访谈显示「推荐 = 必须」的误读普遍 | `docs/directions/recommended-realm-direction-note.md` §3 |
| 技能触发演出增强 | 触发条件 = 真人测试反馈「战斗存在感弱 / 像幻灯片 / 感知不到技能」；边界 = 只加演出，不引入手动操作、不加战斗内资源、不改触发参数 | `docs/directions/wuxue-collection-direction-note.md` §4 |

> **注**：本表五项中有三项（打字机、推荐境界方案 b、触发演出）的评审输入直接来自 MVP-0 外部测试数据——这是「实现门禁 = 测试报告出具」的结构性理由之一，不是流程装饰。

### 2.3 明确不包含

以下系统为 MVP-1 明确范围外，不能因为「顺手」加入：

| 系统 | 不做依据 |
|---|---|
| 经脉/窍穴（全部三方案） | `meridian-direction-note.md` §4：「三个方案均不进 MVP-0/MVP-1」，完整版方向 |
| 武学收集 + 星级成长 | `wuxue-collection-direction-note.md` §1：记录为**完整版**方向 |
| 战斗内手动/概率释放主动技、蓝条 | `wuxue-collection-direction-note.md` §1：「拒绝（MVP-0 **与 MVP-1**）」，理由沿用规格书 §7.1 模型锁定三条 |
| 「归隐后先回新一轮再引导声望阁」流程 | `retire-typewriter-direction-note.md` §1：「已拒绝……记录在案，防止日后重提」 |
| 图鉴、奇遇、门派长期声望 | MVP-0 规格书 §11.2：MVP-2 范围 |
| 留存（7 日等）、商业化转化 | MVP-0 规格书 §10.3 沿用；MVP-1 验证的是回访**节拍**，不是留存 KPI |
| 速度/行动条属性 | MVP-0 规格书 §7.1 预留「若测试证明需要速度轴，MVP-1 再评估」——**默认不做**，仅当首测出现相应证据才评审 |

### 2.4 炼丹 / 秘境：默认不进，唯一例外条件

MVP-0 规格书 §11.2 原文是「MVP-0 验证通过后，才考虑 MVP-1 加入简单炼丹、基础离线收益**或**一个小型秘境」——这是**选择列表，不是打包清单**。本文裁定：

- **MVP-1 最小范围 = 基础离线收益一个系统**。头号假设只需要它即可验证；加系统违反 MVP-0 确立的「不要为未来系统提前增加复杂实现」（规格书 §11.2）与「只做必要系统」支柱。
- **唯一例外条件**：MVP-1 测试（或纸面模拟）发现**回访会话缺决策点**——即初案 §十四「每次会话必须包含至少一个决策点」无法由既有决策（资源投向、升武学、突破、挑战 Boss、调整构筑）满足时，才评审加入炼丹或小型秘境**之一**作为回访决策载体，且二选一、不同时进。
- 声望商店平台期衔接（§2.1）优先用**追加声望节点**解决，不自动升级为新系统。

---

## 3. 核心循环规格：回访会话

### 3.1 单次回访会话（3–10 分钟 `[待定-重推批次]`）

```text
上线 → 出关结算（离线收益入账，一屏看懂）→ 回归检查（进度/资源/卡点现状）→ 决策投入（升级/突破/挑战/调整/资源投向）→ 设定下次目标（可预期「下次回来能做什么」）→ 离线（闭关开始）
```

- **紧张感来源**：离线上限（§5）——收益封顶后继续离线是浪费，构成回访动机；但不做惩罚性机制（无「不回来就扣东西」）。
- **胜利状态**：会话内完成至少一个有意义决策，并对下次回访形成预期。
- **失败状态**：上线发现无事可做（空会话）——这是 §8.2 表的头号失败现象，不是玩家的错。

### 3.2 单日循环（2–3 次会话 `[待定-重推批次]`）

早/晚各一次回访为基准想象，节奏由离线上限与产出速率共同调节。会话间隔与离线上限的匹配关系是数值重推批次的核心命题（间隔 > 上限 = 浪费感；间隔 << 上限 = 上限形同虚设）。

### 3.3 长期循环（跨日与轮次）

MVP-0 的「归隐 → 第二轮更快」循环原样保留，叠加维度是：**轮长从单会话拉长为多会话跨日**（锚点见 §4），归隐决策从「本次会话内的终点」变为「若干天节奏后的里程碑」。prestige 循环与放置节拍在此汇合——这也是两个假设必须分开验证的原因：汇合处的任何失败都需要能归因到单独一环。

---

## 4. 会话结构与轮长锚点

> 本节全部数字为**讨论起点**，出自初案 §十四（其表内原生 `[待测试]` 标记原样有效）；MVP-1 实际取值一律标 `[待定-重推批次]`，由数值表重推批次定值。

### 4.1 轮长锚点

初案 §十四单轮时长曲线：第 1 轮 30–60 分钟（= MVP-0）、第 2 轮 1–2 小时、第 3–5 轮半天–2 天。MVP-1 验证放置节拍需要**轮长跨越至少一次真实离线间隔**，因此轮长锚点取初案第 2–3 轮尺度 `[待定-重推批次]`（开放问题 1，见 §10）。

### 4.2 重推方法论（引用，不重复造）

按量级边界文档口径执行：

- **「怎么算」保留，「算多大」不保留**——公式、规则、守则、曲线形状带进 MVP-1；具体数字、配比、时长全部按新轮长锚点重新反推（量级边界文档「一句话原则」）。
- 重推方式 = 换锚点后按各表头部的「推导锚点」重推一遍，规则层原样沿用（同文档「对后续工作的含义」第 2 条）。
- 任何新增数值/系统先过规则层四项检查：加成域归属、乘区是否超 2、资源三列账是否闭合、是否触碰门槛 Boss 守则与 ≤5 分钟节拍红线（同文档第 3 条）。
- ≤5 分钟可见进展红线是**在线尺度**约束；其在离线/回访节拍下的等价约束（回访会话内多久必须有一次可见进展）由本次验证的结论另定（`zhoutian-direction-note.md` §6 第 3 项预留的同一问题）。

---

## 5. 离线收益规则（方向锁定，数值待定）

以初案 §十四「离线收益规则」为讨论起点，规则方向在此锁定，数值全部 `[待定-重推批次]`：

| 规则 | 方向（锁定） | 数值 |
|---|---|---|
| 产出范围 | 离线只产出基础资源：内力、银两、少量阅历 | 比例 `[待定-重推批次]` |
| 决策保留 | 离线**不**自动突破、**不**自动归隐、**不**获得首通奖励、**不**触发稀有奇遇、**不**推进 Boss——所有关键决策必须留给在线时刻 | —（规则无数值） |
| 时长上限 | 有上限，收益封顶构成回访动机 | 初始 4 小时 `[待定-重推批次]`；声望天赋提升档位（8/12/24h）属完整版离线域设计，MVP-1 是否放入首档由重推批次定 |
| 离线效率 | 低于在线挂机，且任何加成后不得超过在线 100% | 60%–80% `[待定-重推批次]` |
| 加成归属 | 离线相关加成统一归属「离线域」（初案 §四加成域体系），不与既有永久/本轮乘区混淆 | —（归属规则无数值） |
| 结算时点 | 回归上线时一次性结算并呈现（出关结算）；**离线期间不做后台实时结算** | — |

> **与 MVP-0 实现的衔接注记**：MVP-0 实现路线图（规格书 §12）明文「页面关闭期间不结算任何收益」，该条款的存在目的就是防止离线收益在 MVP-0 阶段被顺手实现、污染本假设。MVP-1 开工（过门禁后）时解除该条款，解除动作本身列入实现路线图首项。

---

## 6. 闭关—出关结算呈现（设计要求，非实现细节）

放置节拍的头号自解释关口是**出关结算**——玩家离开 8 小时后回来的第一屏，决定「回归检查」这一环是否成立。硬性要求（类比 MVP-0 §8.6 的写法，细化归呈现层规格交付物）：

1. **一屏看懂**：闭关时长、获得资源明细、是否触顶（上限已满的明确提示）——三项信息一屏内呈现，不埋层级。
2. **数值可信**：结算数字 = 结算函数实际输出，可核对（时长 × 速率 × 效率的构成可查）——PRODUCT.md 原则 1 在离线场景的直接延伸。
3. **不越权确认**：结算屏应让玩家确认「闭关没有替我做决定」（无突破、无战斗推进），与 §5 决策保留规则互为表里。
4. **衔接决策**：结算屏关闭后直接落在一个可行动的决策入口（资源投向/突破就绪/挑战入口），30 秒内有事可做 `[待定]`——对齐「回访有决策」支柱；具体时延指标由 MVP-1 埋点规格定义。
5. **术语纪律**：玩家侧一律「闭关 / 出关结算」；「离线收益」是内部机制代号，不出现在界面文案（沿用 MVP-0 §6.1 机制与皮肤分层管理的既有做法）。

---

## 7. 与 MVP-0 的关系

### 7.1 规则层沿用清单（原样带入，不重推）

以下规则按量级边界文档「保留到完整版的：规则和结构」表原样沿用（出处见该表）：

1. 公式形态：双曲防御 `100/(100+DEF)`、命中率 `命中/(命中+闪避)` 下限 30%（形状保留，常数复查）。
2. 战斗结算规则：固定回合制；毒无视防御且绕过护盾；反伤「减免后、护盾吸收前」；六状态结算时机（MVP-0 规格书 §7.1/§7.2）。
3. 曲线形态：指数基础曲线、段内固定底数 + 段间跃迁节点、敌人曲线锚定玩家基础曲线。
4. 数值全局规则：每最终量乘区 ≤2、同域加法合并、4 加成域域间不相乘、禁新增乘区作成长感手段（初案 §四，明文为完整版全局规则）。
5. 归隐结构：门槛锚定推关里程碑、首次后常驻、声望公式方向。
6. 设计守则：门槛 Boss 只做数值检查；≤5 分钟可见进展节拍红线（在线尺度，离线等价约束另定，见 §4.2）。
7. 经济原则：每种资源来源/主消耗/后期 sink 三列账；不没收不可再生资源。
8. **MVP-0 全部已实现系统作为基底原样保留**：5 境界 / 3 图 / 3 Boss / 三路线 / 归隐 / 8 声望节点 / 周天呈现体系——MVP-1 不改其规则，只在数值重推批次换其数字。

### 7.2 MVP-0 结论的效力边界

- prestige 循环成立（自玩 + sim 已证数值层；主观层待外部测试确认）≠ 放置节拍成立——本文 §1.1 引用的 §1.2 原文即此边界的权威表述。
- MVP-0 测试报告中的任何主观结论（自解释性、归隐情绪、流派认知）只对**主动循环**有效力；出关结算的自解释性是全新命题，须由 MVP-1 测试单独取证。
- 反向同理：MVP-1 测试若失败，优先归因放置节拍层（回访/结算/决策点），不自动否定已验证的主动循环——归因隔离是两阶段拆分的全部意义。

### 7.3 测试形式差异预告

- MVP-0：90–120 分钟单人陪同制（测试方案 §1）。**该形式无法测出放置节拍**——陪同环境里不存在真实的「离开与回来」。
- MVP-1：**多日远程 / 日记式**——测试者在自己的日常环境自然回访，采集回访时间分布、会话时长、出关结算行为与日记式主观记录。样本量、天数、报酬结构、装置效应控制（日记任务本身诱发回访的偏差）由《MVP-1 测试执行方案》另立定义（交付物 ⑥）。
- 埋点前提：跨会话/跨日数据采集超出 MVP-0 埋点「一次性本地导出」的设计范围，按其范围纪律另立《MVP-1 埋点与度量规格》（交付物 ⑤）。

---

## 8. MVP-1 成功与失败标准（草案）

> 本节数值目标全部 `[待定]`，定值时机 = 数值表重推批次（届时同步写入 MVP-1 埋点规格的推导锚点表）。判定档位（x/n 口径）由《MVP-1 测试执行方案》定义。

### 8.1 设计验证成功

若测试中出现以下结果，可认为放置节拍方向成立：

1. **回访意愿**：测试者在无外部提醒（日记任务除外，其装置效应由测试方案控制）下自发回访，日均回访次数落在目标区间 `[待定]`，且访谈表述回访动机为「想看攒了多少 / 想做某件事」而非「因为要交作业」。
2. **出关结算自解释**：测试者无指导看懂出关结算——能复述「闭关多久、拿了什么、有没有到顶」三要素；对结算数字无「被坑/算不明白」类质疑（数值可信）。
3. **回访会话有决策**：每次回访会话包含 ≥1 个有意义决策（判据类比 MVP-0 埋点规格 §1.2 的调整事件口径，由 MVP-1 埋点规格定义），空会话占比低于阈值 `[待定]`。
4. **上限机制被理解**：测试者能说出「闭关有上限，攒满就亏了」——离线上限作为回访动机被认知，而非被体验为惩罚。
5. **节奏形成**：测试期后半段，测试者能预期「下次上线能做什么」（访谈判定）；回访间隔分布趋向与离线上限匹配 `[待定]`。
6. **在线时刻仍有价值**：测试者未把游戏退化为「纯上线收菜」——在线会话中仍发生挑战/调整/突破类行为（离线效率 < 在线 100% 的规则是否被体感支持）。

### 8.2 设计验证失败

若出现以下任一情况，应优先调整 MVP-1 放置层，而不是继续扩系统：

| 失败现象 | 可能原因 | 处理方向 |
|---|---|---|
| 上线只点一下就走（空会话） | 回访决策点不足 | 先查既有决策（资源投向/突破/挑战）的呈现衔接（§6-4）；仍不足则触发 §2.4 唯一例外条件，评审炼丹/秘境之一 |
| 出关结算看不懂 / 被质疑「算错了」 | 结算构成不透明或呈现层级过深 | 按 §6-1/-2 重做结算屏信息架构；数值可信是红线，不许用演出掩盖 |
| 测试者不回访（间隔远超上限） | 回访动机不成立：上限太宽 / 收益预期太弱 / 无牵挂目标 | 收紧离线上限或强化「下次目标」呈现；若访谈显示根本不想回来，按 §7.2 归因隔离核查是循环层还是节拍层问题 |
| 回访过密（远小于上限，接近在线挂机） | 上限/速率错配，离线形同虚设 | 重推上限与产出速率匹配关系（§3.2 命题）；确认非日记任务装置效应 |
| 离线被体验为惩罚（「不在线就亏」焦虑） | 效率折扣感知过重或触顶浪费感过强 | 调效率比或触顶前提示；不引入「不回来就扣东西」类机制（§3.1 底线） |
| 在线时刻失去意义（纯收菜化） | 离线效率过高或在线独占内容太薄 | 压离线效率（≤ 在线 100% 是硬规则，实际值应显著低于）；检查在线独占决策（Boss/首通/突破）的密度 |
| 回访后无进展感（会话内无可见变化） | 离线产出量不足以支撑会话内一次投入 | 重推产出/消耗配比，使单次标准离线期 ≈ 至少一次有意义投入 `[待定]` |
| 归隐节奏被离线打乱（多日轮长下归隐门槛遥不可及或秒达） | 轮长锚点与离线速率错配 | 回 §4.1 重定轮长锚点；prestige 层规则不动，只动数字 |

### 8.3 MVP-1 不用验证的指标

- 7 日/30 日留存、商业化转化（沿用 MVP-0 §10.3）。
- 主动循环的自解释性（MVP-0 测试报告已覆盖；若该报告出现红旗，先修后测，不由 MVP-1 重验）。
- 完整版轮长曲线（中后期 1–3 天/轮）、多门派长期声望、MVP-2 内容。
- 移动端形态：PRODUCT.md 记录碎片会话「桌面浏览器优先」，移动端优先级重估属完整版（量级边界 UI 节），MVP-1 沿用桌面。

---

## 9. 交付物路线图

按三问裁决排定顺序（所有者已批准），本文为其中第 ② 项：

- [ ] ① **MVP-0 测试报告**（外部测试产物；②以下多项的评审输入）：§10.1 逐条 x/n + §10.2 八现象对照 + 每人四里程碑提速原始值 + 访谈汇总 + 降档敞口声明。**实现门禁的钥匙。**
- [x] ② **MVP-1 核心循环规格书**（本文，v0.1 草案已交付；**定稿以 ① 的结论为输入**——尤其 §2.2 三个依赖首测数据的触发项与 §8 标准的最终措辞）。
- [ ] ③ **数值表重推批次**：换轮长锚点重推公式/内容/经济表；离线上限/效率数值化（§5）；周天两项候选评审（§2.2）；商店平台期补节点。
- [ ] ④ **呈现层规格**：闭关—出关结算视图 + 回归检查视图（§6 硬性要求的细化）；tooltip 方案 a；§2.2 条件项按 ① 报告逐个裁决。
- [ ] ⑤ **MVP-1 埋点与度量规格**（另立文档，跨会话/跨日采集，§8 各标准的口径定义处）。
- [ ] ⑥ **MVP-1 测试执行方案**（多日远程/日记式；样本、天数、装置效应控制、判定档位）。

> **门禁重申**：③–⑥ 的产出物落地与一切实现开工，以 ① 出具且无 §10.2 红旗为前提；①进行期间只允许 ② 的纸面迭代。

---

## 10. 开放问题（待所有者拍板）

| # | 问题 | 本文暂用口径 | 需拍板的原因 |
|---|---|---|---|
| 1 | **轮长锚点取哪一档**：初案第 2 轮（1–2 小时，2–3 天测试窗口）还是第 3–5 轮（半天–2 天，一周以上测试窗口）？ | 倾向第 2–3 轮尺度（§4.1） | 直接决定重推幅度与 MVP-1 测试周期/成本 |
| 2 | **内容量是否扩**：MVP-1 是否维持 3 图 5 境界只换数字拉长轮长（内容被稀释），还是允许最小内容增量（平台期衔接节点之外）？ | 不扩内容，只换锚点（§2.1/§2.4） | 「旧内容拉长轮长」的稀释感是体验风险，接受与否是所有者的取舍 |
| 3 | **炼丹/秘境例外条件的评审时点**：纸面模拟发现决策点不足即评审，还是必须等 MVP-1 首测实证？ | 倾向纸面模拟即可触发评审、首测实证才触发实装 | 影响 ③ 批次是否预留该系统的数值坑位 |
| 4 | **离线上限声望天赋档位（8/12/24h）**是否随 MVP-1 首发（作为平台期新节点），还是首发只有固定 4h？ | 倾向首发固定上限，档位留给衔接节点评审（§5） | 涉及声望经济平台期方案的方向 |
| 5 | **MVP-1 测试预算口径**：多日日记式测试的报酬结构与人数上限（MVP-0 是 90–120 分钟陪同有偿的一次性支出，MVP-1 是多日跟踪） | 未定，留给 ⑥ | 单人项目的真金白银约束，方法论须匹配预算 |

---

## 11. MVP-1 一句话验收标准

如果测试者在自己的日常环境里，愿意隔着真实的几小时回来看一眼闭关收成、看得懂、且每次回来都有一件值得做的事并开始预期下一次——那么放置节拍成立；否则不要扩系统，先修回访这一环。
