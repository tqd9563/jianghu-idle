# 《江湖无尽录》MVP-1 离线收益数值增补表

> **版本**：v0.2（按所有者确认：资源口径为内力 / 银两 / 阅历；按当前最大可挂机关卡驱动；当前无在线挂机收益表；首发不接 VIP / 月卡 / 广告 / 特权）
> **日期**：2026-07-06
> **上游文档**：`mvp-1-core-loop-spec.md`（v0.3 草案）§4.1 / §5 / §8.1；`mvp-0-formula-tables.md`、`mvp-0-content-tables.md`。
> **范围**：MVP-1 基础离线收益。本文只补「离线收益」所需的上限、效率、产出比、刷新阈值与反作弊 / 时钟边界口径；不重推 MVP-0 既有公式表 / 内容表，不引入商业化加成，不新增装备、奇遇、秘境、炼丹等系统。
> **数值纪律**：全部为首版可实现值；`[待测试]` 表示测试敏感项，实测后优先调整，但当前值即为实现 / 模拟起点。

---

## 0. 变更日志

| 版本 | 日期 | 变更内容 |
|---|---|---|
| v0.1 | 2026-07-06 | 通用离线收益表提案：包含离线效率、上限、追赶倍率、资源拆分、概率掉落、VIP/特权钩子与反作弊字段。 |
| v0.2 | 2026-07-06 | 项目口径收敛：核心资源改为内力 / 银两 / 阅历；主驱动改为当前最大可挂机关卡；因当前无在线挂机收益表，本文直接给首发基础离线收益；删除 VIP / 月卡 / 广告 / 特权表；稀有掉落仅保留为空扩展，不进首发核心验收。 |

---

## 1. 推导锚点

本文继承 `mvp-1-core-loop-spec.md` v0.3 的裁决：MVP-1 当期只交付离线功能，不验证放置节拍假设；MVP-0 的 60 分钟压缩锚点不动，全量换锚点重推后置。

| 锚点 | 本表处理 |
|---|---|
| 不破坏 MVP-0 主循环 | 离线只发基础资源，不推进关卡 / Boss / 归隐 / 突破 / 首通。 |
| 单次满额离线收益 ≈ 一次会话级投入 | 首发表把满额收益压在「一次武学升级或一段周天充能」量级，显著小于一轮总需求。 |
| 当期可验收 | 离线上限取压缩锚点可测值，并提供调试覆盖值，支撑 §8.1 A1–A7 单日验收。 |
| 资源三列账闭合 | 离线只作为内力 / 银两 / 阅历的新增来源列；不新增付费乘区，不接商业化倍率。 |
| 玩家侧术语纪律 | 机制内部称「离线收益」；界面文案使用「闭关 / 出关结算」，不直接出现「离线收益」。 |

---

## 2. 结算规则

### 2.1 主公式

```text
有效闭关分钟 = clamp(原始离线分钟, 0, 离线上限分钟)

基础资源收益 = 当前最大可挂机关卡所属档位的每分钟产出
  × 有效闭关分钟
  × 离线效率
  × 追赶倍率

最终发放 = floor(基础资源收益)
```

### 2.2 驱动字段

主驱动为「当前最大可挂机关卡」，不是玩家等级。

```text
current_max_idle_stage = 玩家当前已解锁、且系统允许作为挂机收益来源的最高关卡
```

首发实现可直接使用当前主线最高可重复收益关卡作为该值；若未来出现「已通关但不可挂机」或「推荐挂机点」系统，再把该字段改为显式存档字段。

### 2.3 离线期间绝不自动执行

以下规则来自 `mvp-1-core-loop-spec.md` §5 / §8.1-A2，本文仅落数值表口径：

- 不自动突破：丹田内力达到突破消耗时，只点亮按钮，不自动扣款。
- 不自动归隐：保底触发器不计离线时长。
- 不获得首通奖励。
- 不推进 Boss / 关卡。
- 不触发稀有奇遇。
- 不运行在线自动连战。

---

## 3. 表 A：离线收益关卡档位 `offline_reward_stage`

### 3.1 字段定义

| 字段名 | 中文名 | 类型 | 示例 | 说明 |
|---|---:|---:|---:|---|
| `id` | 配置 ID | int | 1001 | 唯一 ID。 |
| `idle_stage_min` | 最大可挂机关卡下限 | int | 1 | 闭区间。 |
| `idle_stage_max` | 最大可挂机关卡上限 | int | 4 | 闭区间。 |
| `jianghu_phase` | 江湖阶段 | string | 村外小径·试手 | 策划可读分段名。 |
| `neili_per_min` | 内力 / 分钟 | int | 90 | 离线基础内力产出。 |
| `silver_per_min` | 银两 / 分钟 | int | 4 | 离线基础银两产出。 |
| `experience_per_min` | 阅历 / 分钟 | float | 0.6 | 离线基础阅历产出，允许小数，最终 floor。 |
| `offline_efficiency` | 离线效率 | float | 0.35 | 相对本表基础速率的折扣。首发不对齐在线收益表，因为在线挂机收益表尚不存在。 |
| `offline_cap_min` | 离线上限分钟 | int | 20 | 首发正式上限。 |
| `debug_cap_min` | 调试上限分钟 | int | 10 | 验收 A4 用，非正式配置。 |
| `min_settle_min` | 最小结算分钟 | int | 3 | 低于该值不弹出出关结算。 |
| `catchup_per_stage` | 单关追赶系数 | float | 0.000 | 首发默认 0，字段保留。 |
| `catchup_max` | 追赶上限 | float | 0.000 | 首发默认 0，字段保留。 |
| `drop_group` | 掉落组 | string | none | 首发默认 `none`。 |
| `notes` | 备注 | string | 新手前段 | 调参备注。 |

> **字段说明**：`offline_efficiency` 首发仍保留，是为了后续一旦补在线挂机收益表，可把本表基础速率改为在线速率派生；当前版本中它只是独立调参旋钮。

### 3.2 起始配置

| id | 最大可挂机关卡 | 江湖阶段 | 内力/分 | 银两/分 | 阅历/分 | 离线效率 | 正式上限 | 调试上限 | 最小结算 | 追赶/关 | 追赶上限 | 掉落组 | 备注 |
|---:|---:|---|---:|---:|---:|---:|---:|---:|---:|---:|---:|---|---|
| 1001 | 1-4 | 村外小径·试手 | 90 | 4 | 0.60 | 0.35 | 20 | 10 | 3 | 0.000 | 0.00 | none | 刚开始只给轻量闭关反馈，不跳过早期教学。 |
| 1002 | 5-8 | 村外小径·破寨 | 120 | 6 | 0.90 | 0.35 | 20 | 10 | 3 | 0.000 | 0.00 | none | Boss 1 前后；满额约 840 内力。 |
| 1003 | 9-12 | 洛阳近郊·入城 | 155 | 8 | 1.10 | 0.34 | 22 | 10 | 3 | 0.000 | 0.00 | none | 进入地图 2，银两略升。 |
| 1004 | 13-18 | 洛阳近郊·鏖战 | 190 | 10 | 1.40 | 0.34 | 22 | 10 | 3 | 0.000 | 0.00 | none | 覆盖精英教学段。 |
| 1005 | 19-22 | 华山古道·登山 | 230 | 12 | 1.70 | 0.33 | 24 | 12 | 3 | 0.000 | 0.00 | none | Boss 2 后，单次满额可支撑一次中段投入。 |
| 1006 | 23-25 | 华山古道·遇险 | 270 | 14 | 2.00 | 0.33 | 24 | 12 | 3 | 0.000 | 0.00 | none | 精英密集段，阅历补少量参悟压力。 |
| 1007 | 26-27 | 华山古道·临寨 | 315 | 16 | 2.30 | 0.32 | 26 | 12 | 3 | 0.000 | 0.00 | none | Boss 3 前夕，仍不得替代在线调整。 |
| 1008 | 28 | 黑风寨主前 | 360 | 20 | 2.80 | 0.32 | 26 | 12 | 3 | 0.000 | 0.00 | none | MVP-0 终段；满额收益必须远小于归隐前总需求。 |

### 3.3 满额收益校验

按 `每分钟产出 × 离线效率 × 正式上限` 粗算：

| id | 最大可挂机关卡 | 满额内力 | 满额银两 | 满额阅历 | 校验口径 |
|---:|---:|---:|---:|---:|---|
| 1001 | 1-4 | 630 | 28 | 4 | 小于境界 2 突破消耗 2,800；不会跳过第一次核心投入。 |
| 1002 | 5-8 | 840 | 42 | 6 | 可支撑早期一段周天或低级武学升级。 |
| 1003 | 9-12 | 1,159 | 59 | 8 | 约等于一次中低级投入，不足以直接越过 Boss 2 调整链。 |
| 1004 | 13-18 | 1,421 | 75 | 10 | 与武学 lv4 单级成本同量级。 |
| 1005 | 19-22 | 1,822 | 95 | 13 | 可形成回归后一次操作，但不替代境界 4→5 积累。 |
| 1006 | 23-25 | 2,138 | 111 | 15 | 接近 lv7 单级成本，仍小于一段境界 5 周天 4,200。 |
| 1007 | 26-27 | 2,621 | 133 | 19 | 高段闭关有感，但不能直接打通终局。 |
| 1008 | 28 | 2,995 | 166 | 23 | 仍低于境界 5 充能段 4,200，满足「一次会话级投入」预算线。 |

> **红线**：首发任一档满额内力不应超过 4,200（境界 5 单段周天充能），否则压缩锚点下容易把离线变成跳过整段在线张力的按钮。

---

## 4. 表 B：可选掉落组 `offline_drop_group`

首发核心验收不启用掉落，所有 `drop_group = none`。本表只保留未来扩展结构，避免实现时临时发明字段。

| 字段名 | 中文名 | 类型 | 示例 | 说明 |
|---|---:|---:|---:|---|
| `drop_group` | 掉落组 ID | string | none | `none` 表示不掉落。 |
| `item_id` | 道具 ID | string | herb_001 | 未来材料 / 残页等。 |
| `item_name` | 道具名 | string | 止血草 | 策划可读名。 |
| `unlock_idle_stage` | 解锁可挂机关卡 | int | 12 | 达到后才参与掉落。 |
| `check_interval_min` | 判定间隔 | int | 30 | 每 N 分钟判定一次。 |
| `drop_rate` | 单次概率 | float | 0.10 | 0.10 = 10%。 |
| `min_count` | 最小数量 | int | 1 | 成功时数量下限。 |
| `max_count` | 最大数量 | int | 1 | 成功时数量上限。 |
| `single_offline_cap` | 单次离线上限 | int | 2 | 单次最多掉落。 |
| `daily_cap` | 每日上限 | int | 4 | 单日最多掉落。 |
| `notes` | 备注 | string | 首发不启用 | 调参备注。 |

首发固定行：

| drop_group | item_id | item_name | unlock_idle_stage | check_interval_min | drop_rate | min_count | max_count | single_offline_cap | daily_cap | 备注 |
|---|---|---|---:|---:|---:|---:|---:|---:|---:|---|
| none | — | — | — | — | — | — | — | — | — | 首发不启用掉落；出关结算隐藏「机缘」区。 |

---

## 5. 表 C：离线结算约束 `offline_settlement_rule`

### 5.1 字段定义

| 字段名 | 中文名 | 类型 | 首发值 | 说明 |
|---|---:|---:|---:|---|
| `rule_id` | 规则 ID | string | default | 唯一 ID。 |
| `min_settle_sec` | 最小弹窗结算秒数 | int | 180 | 低于该值不展示出关结算。 |
| `silent_grant_below_min` | 短离线是否静默入账 | bool | true | 低于最小结算但大于 0 时，可静默入账或直接忽略；首发静默入账。 |
| `max_single_offline_min` | 单次最大正式离线分钟 | int | 26 | 与表 A 最高档一致。 |
| `debug_cap_min` | 调试覆盖上限分钟 | int | 10 | 验收 A4 可临时覆盖为 10–30。 |
| `negative_time_policy` | 负时间策略 | string | zero_reward | 系统时间回拨时按 0 处理。 |
| `forward_time_policy` | 前拨时间策略 | string | clamp_to_cap | 系统时间前拨或跨时区时按上限截断。 |
| `settle_once_policy` | 单时段结算策略 | string | consume_timestamp_once | 同一离线时段只结算一次。 |
| `daily_cap_enabled` | 是否启用每日上限 | bool | false | 单机首发不做每日上限，避免额外日期状态。 |
| `notes` | 备注 | string | 本地单机只保证不崩不负不超限 | 不做强反作弊。 |

### 5.2 首发配置

| rule_id | min_settle_sec | silent_grant_below_min | max_single_offline_min | debug_cap_min | negative_time_policy | forward_time_policy | settle_once_policy | daily_cap_enabled | 备注 |
|---|---:|---|---:|---:|---|---|---|---|---|
| default | 180 | true | 26 | 10 | zero_reward | clamp_to_cap | consume_timestamp_once | false | 对齐 `mvp-1-core-loop-spec.md` §8.1 A3/A5/A6：不负、不超限、不重复结算。 |

---

## 6. 追赶倍率口径

首发不启用追赶倍率，表 A 中 `catchup_per_stage = 0`、`catchup_max = 0`。

保留字段的原因：回归检查界面已经需要「追赶 / 上限 / 效率」信息槽位；未来内容量扩充后若进入自然游玩期，可不改表结构直接开启。

未来启用公式预留：

```text
进度差 = 推荐挂机进度 - 当前最大可挂机关卡
追赶倍率 = 1 + min(进度差 × catchup_per_stage, catchup_max)
```

未来启用限制：

- 只作用于内力 / 阅历，不作用于稀有掉落。
- 首次启用上限不得超过 +30%，需重新跑预算红线。
- 若启用追赶，出关结算必须展示构成：基础收益、效率、追赶加成、上限截断。

---

## 7. 程序配置落地建议

本文为设计表，不是运行时配置。待本表通过评审后，再落到代码：

```text
code/src/engine/offlineRewards.ts
code/src/engine/offlineRewards.test.ts
```

建议导出结构：

```ts
type OfflineRewardStage = {
  id: number;
  idleStageMin: number;
  idleStageMax: number;
  jianghuPhase: string;
  neiliPerMin: number;
  silverPerMin: number;
  experiencePerMin: number;
  offlineEfficiency: number;
  offlineCapMin: number;
  debugCapMin: number;
  minSettleMin: number;
  catchupPerStage: number;
  catchupMax: number;
  dropGroup: "none";
};
```

实现侧至少需要以下纯函数：

| 函数 | 职责 |
|---|---|
| `findOfflineRewardStage(currentMaxIdleStage)` | 按最大可挂机关卡匹配表 A。 |
| `getEffectiveOfflineMinutes(lastSeenAt, now, capMin)` | 计算有效闭关分钟，负时间归 0，超时截断。 |
| `calculateOfflineRewards(input)` | 输出内力 / 银两 / 阅历、是否触顶、构成因子。 |
| `shouldShowOfflineSettlement(effectiveMinutes, minSettleMin)` | 判断是否弹出出关结算。 |

测试至少覆盖 `mvp-1-core-loop-spec.md` §8.1 A1 / A3 / A5 / A6。

---

## 8. 验收清单映射

| 规格验收项 | 本表输入 |
|---|---|
| A1 出关结算数值链路对账 | 表 A 每分钟产出、离线效率、上限；表 C 时钟策略。 |
| A2 决策保留 | 本文 §2.3；无表值，属于规则验收。 |
| A3 触顶行为 | 表 A `offline_cap_min`；表 C `forward_time_policy = clamp_to_cap`。 |
| A4 当日多次触顶用例 | 表 A / 表 C `debug_cap_min`。 |
| A5 关页 / 刷新时序边界 | 表 C `settle_once_policy`、`min_settle_sec`。 |
| A6 时钟边界 | 表 C `negative_time_policy`、`forward_time_policy`。 |
| A7 MVP-0 回归不破坏 | 本表不改 MVP-0 既有数字；实现时需跑 sim golden / 既有测试。 |

---

## 9. 开放问题

以下问题不阻塞首发表落地，但会影响后续实现与调参：

1. `current_max_idle_stage` 在存档中的权威来源是哪一个字段？若当前没有，需要实现时补显式字段。
2. 短离线低于 3 分钟时是「静默入账」还是「完全不结算」？本文首发取静默入账，避免玩家刷新损失资源。
3. 表 A 的基础速率未来是否反向成为在线挂机收益表的原型？如果是，后续需拆 `base_per_min` 与 `offline_efficiency` 的语义。
4. 出关结算是否展示小数阅历构成？本文建议最终发放 floor，构成明细可显示精确到 1 位小数。
5. 首发是否需要把 `debug_cap_min` 做成开发开关，而不是配置常量？验收 A4 需要能临时压低上限。

---

## 10. 首轮调参优先级

如果实现后体验不符合预期，按以下顺序调：

1. **先调 `offline_cap_min`**：决定回访触顶压力与单次爆发量。
2. **再调 `offline_efficiency`**：统一缩放收益，不改变各阶段相对结构。
3. **最后调三资源每分钟产出**：用于修正具体资源短缺或过剩。

不要在首轮引入商业化倍率、稀有掉落或追赶倍率来修正基础手感；这些会扩大归因面，违反 MVP-1「功能正确性交付」的当期目标。
