# MVP-1 功能验收清单（⑥″ 定稿）

> **版本**：v1.0
>
> **日期**：2026-07-07
>
> **状态**：验收口径冻结（开工授权 = 所有者 2026-07-07 开工指令；门禁 ①″ 收口记录已出具、③″ 增补表 v0.2 已确认）
>
> **范围**：MVP-1 离线功能交付的阶段 A 验收（A1–A7 全项通过 = 交付成立）
>
> **上游**：`spec.md` §8.1；`offline-rewards.md` v0.2 表 A / 表 C；`telemetry.md`（对账事件）

---

## 0. 变更日志

| 版本 | 日期 | 变更内容 |
|---|---|---|
| v1.0 | 2026-07-07 | 首版定稿：A1–A7 阈值从增补表取数，登记五条实现口径。 |

---

## 1. 验收环境

- 压缩锚点（`spec.md` §4.1）+ `offline-v0.2` 数值表（`TABLES_VERSION` 含 `offline-v0.2`）。
- 调试通道：`#offlinecap=10` 压低离线上限（分钟，持久生效），`#offlinecap=0` 清除；验收记录须裸露 `debug_cap` 位。
- 允许边验边修，修后该项重跑。

## 2. A1–A7 判据（含定稿阈值）

| # | 验收项 | 定稿判据 | 自动化覆盖 |
|---|---|---|---|
| A1 | 出关结算数值链路对账 | 发放 = `floor(每分钟产出 × 有效闭关分钟 × 效率)` 截断至上限；结算屏 UI、`offline_settled` 事件、`calculateOfflineRewards` 输出三处同源同值；三因子构成结算屏可查 | `offlineRewards.test.ts` 满额 8 档逐格 + store 入账与事件同值（UI 项待结算屏落地后人工核对） |
| A2 | 决策保留逐条 | 离线不自动突破（丹田 ≥ 全额只点亮按钮）、不自动归隐（保底两触发器不计离线时长）、不获首通、不推进 Boss/关卡、不触发奇遇、不运行自动连战；出关后除三资源外一切状态与离线前一致 | store 集成测试（realm / clearedStages / runPlaySec / lastProgressSec 全量断言） |
| A3 | 触顶行为 | 达上限停止累计（不溢出不为负）；结算屏显示「已触顶」；触顶结算后再离线重新从零累计 | 引擎 clamp + A4 循环测试 |
| A4 | 当日多次触顶用例 | `#offlinecap=10` 后「离线→触顶→结算→再离线」循环 ≥3 次，每次独立正确无跨次串账 | store 集成测试三循环逐次断言 |
| A5 | 关页/刷新时序边界 | 关页→重开恰好一次结算（consume_timestamp_once = 结算后立即持久化刷新 savedAt）；raw < 180s 静默入账不弹屏（表 C `min_settle_sec`）；结算发生在存档恢复之后、玩家可操作之前（init 内完成）；崩溃恢复不丢不重（savedAt 每 5s 随 persist 刷新） | store 集成测试（重复 init 不双结算 / 静默入账） |
| A6 | 时钟边界 | 回拨 → 离线时长按 0（`zero_reward`），不为负不崩溃；前拨/跨时区 → 按上限截断（`clamp_to_cap`）；不做防作弊，只保证不崩不负不超限 | 引擎 + store 双层测试（回拨 1h / 前拨 1 年） |
| A7 | MVP-0 回归不破坏 | sim golden 18 例全过（在线结算路径零改动——tick / resolveBattle 未动）；归隐、声望、既有埋点回归通过；离线不入保底停滞计时与 `run_duration_s` | vitest 全量（79 例）+ A2 断言 |

## 3. 实现口径注记（定稿，验收按此判）

1. **满额对账以主公式 floor 真值为准**：增补表 §3.3 校验表为四舍五入粗算，4 格与 floor 差 1（75→74 / 1822→1821 / 111→110 / 2621→2620）；§2.1 主公式是权威。
2. **热刷新下界**：原始离线 < 5 秒完全跳过（不入账、不发事件），防会话内热刷新事件噪声；5s–180s 静默入账并发 `silent=true` 事件。
3. **观察员暂停中的存档不结算**：暂停冻结一切结算（与 tick 口径一致），时间戳照常消费；暂停横跨离线的时段不产生离线收益。
4. **`charge_segment_full` 补发口径**：出关入账后首个 tick 按新水位一次性连发（同 ts burst），即 A2「一次性补发不轰炸」的事件层表达；UI 提示层不逐段弹提示。
5. **`current_max_idle_stage` 权威来源**（增补表开放问题 1 裁决）：= 已通关的最高关卡全局序号（关卡顺序通关构成前缀，未通任何关取 1），不新增存档字段。
6. **构成公式行仅观察员通道显示**（所有者 2026-07-07 裁决，规格 §6 呈现裁决 ②）：A1「三因子构成在结算屏可查」子项在**观察员模式**（`#observer=1`）下验收；玩家侧默认不渲染公式行。

## 4. 验收记录（⑦″ 执行时填写）

> 待阶段 A 验收执行后逐项填写；出关结算屏 UI 落地前，A1 的「UI 同源」子项与 A3 的「结算屏触顶提示」子项挂起。

| # | 结果 | 日期 | 复跑 | 备注 |
|---|---|---|---|---|
| A1 | 待验收 | — | — | 引擎/埋点两处同源已由测试覆盖；UI 项待结算屏 |
| A2 | 待验收 | — | — | |
| A3 | 待验收 | — | — | |
| A4 | 待验收 | — | — | |
| A5 | 待验收 | — | — | |
| A6 | 待验收 | — | — | |
| A7 | 待验收 | — | — | |

**R7 敞口重申**：本清单只验收离线功能正确性；放置节拍假设（回访意愿 / 自解释 / 决策衔接的心理模型）仍为未验证敞口，挂《收口记录》R7，后置节点 =「内容量扩充后的自然游玩期」。
