# 离线收益数值表

> **版本**：v1.0（从 `../mvp1/offline-rewards.md` v0.2 迁入 + `../mvp1/spec.md` §5/§8.1 口径合并，零变更）
>
> **日期**：2026-07-25
>
> **范围**：离线收益的上限、效率、产出比、刷新阈值与时钟边界
>
> **数值纪律**：全部为首版可实现值

## 推导锚点

| 锚点 | 本表处理 |
|---|---|
| 不破坏 MVP-0 主循环 | 离线只发基础资源，不推进关卡/Boss/归隐/突破/首通 |
| 单次满额离线 ≈ 一次会话级投入 | 满额收益压在「一次武学升级或一段周天充能」量级 |
| 当期可验收 | 离线上限取压缩锚点可测值 |
| 资源三列账闭合 | 离线只作为内力/银两/阅历的新增来源列 |
| 玩家侧术语纪律 | 界面文案用「闭关/出关结算」 |

---

## 1. 结算规则

### 1.1 主公式

```text
有效闭关分钟 = clamp(原始离线分钟, 0, 离线上限分钟)
基础资源收益 = 当前最大可挂机关卡所属档位的每分钟产出 × 有效闭关分钟 × 效率系数
```

### 1.2 决策保留（来自 `../mvp1/spec.md` §5）

离线收益发放后，玩家不自动推进关卡、不自动突破、不自动归隐、不产残页。回访后必须手动做出至少一个真实决策（升武学/推关/冲穴等），离线收益才是合法增益而非跳过核心循环。

### 1.3 验收判据（来自 `../mvp1/spec.md` §8.1）

- A1：离线时长与收益对账（公式输出 vs 界面展示一致）
- A3：离线上限触发时收益不再增长
- A5：刷新页面后收益不丢失、不重复发放
- A6：系统时钟回拨不产生负收益

### 1.4 基础离线上限

| 场景 | 上限 | 效率 |
|---|---|---|
| 在线 | 8 小时 | 100% |
| 离线（关闭页面） | 8 小时 | 50% |
| 调试覆盖 | 可临时压低（`#offlinecap=N`） | — |

---

## 2. 产出档位表（表 A）

离线收益按玩家当前最大可挂机关卡的最高档位取每分钟产出。完整档位表见 `code/src/engine/offlineRewards.ts`（按 `docs/mvp1/offline-rewards.md` 表 A 1:1 实现）。

## 3. 效率系数与上限表（表 C）

按离线时长分段的效率系数，实现见 `code/src/engine/offlineRewards.ts`。

---

## 口径守恒表

| 旧文档 | 新 § | 内容 | 处理 | 核对证据 |
|---|---|---|---|---|
| `mvp1/offline-rewards.md` 表 A/C | §2–§3 | 产出档位表 + 效率表 | 逐字保留（代码实现） | `offlineRewards.ts` 值比对 |
| `mvp1/offline-rewards.md` §2 | §1 | 结算规则 | 逐字保留 | 同上 |
| `mvp1/spec.md` §5/§8.1 | §1.2–§1.3 | 决策保留 + 验收判据 | 合并入此 | `offlineRewards.ts` + `storage.ts` 注释 |
